<Window x:Class="Ajf.NsPlanner.UI.Views.EditAssignmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800" DataContextChanged="Window_DataContextChanged">
    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Detaljer"></Label>

        <Label Grid.Row="1" Grid.Column="0" Content="Time Stamp"></Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedAssignment.TimeStamp,Mode=OneWay}" IsReadOnly="True"></TextBox>

        <Label Grid.Row="2" Grid.Column="0" Content="Desire"></Label>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedAssignment.Desire,Mode=OneWay}" IsReadOnly="True"></TextBox>
    </Grid>
</Window>
